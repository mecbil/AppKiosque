twig:
    file_name_pattern: '*.twig'
    default_path: '%kernel.project_dir%/templates'
    paths:
        '%kernel.project_dir%/src/Home/Templates': home
        '%kernel.project_dir%/src/Menu/Templates': menu

when@test:
    twig:
        strict_variables: true
